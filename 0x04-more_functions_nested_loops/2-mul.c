#include "main.h"

/**
 * mul - perform the multiplication of 2 integers
 * @a: character1 to be multiplied
 * @b: character2 to be multiplied
 *
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
